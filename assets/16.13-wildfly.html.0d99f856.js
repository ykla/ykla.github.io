import{_ as e,o as l,c as d,e as i}from"./app.e67b67d1.js";const s={},t=i(`<h1 id="_16-13-wildfly" tabindex="-1"><a class="header-anchor" href="#_16-13-wildfly" aria-hidden="true">#</a> 16.13 Wildfly</h1><h2 id="installation" tabindex="-1"><a class="header-anchor" href="#installation" aria-hidden="true">#</a> Installation</h2><blockquote><p>There are currently several versions of wildfly10, wildfly11, wildfly12, wildfly13, wildfly14, wildfly15, wildfly16, wildfly17, wildfly18, wildfly24, wildfly25, wildfly26, etc.</p></blockquote><p>Here is an example of wildfly26.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># pkg install wildfly26
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="configure" tabindex="-1"><a class="header-anchor" href="#configure" aria-hidden="true">#</a> configure</h2><p>Add the following code to <code>/etc/hosts</code>, where <code>server02</code> is the local <code>hostname</code> (the host name, set during installation).</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>127.0.0.1 server02
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Some other settings are needed.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># sysrc wildfly26_enable=&quot;yes&quot;
# sysrc wildfly26_args=&quot;-Djboss.bind.address=0.0.0.0 -Djboss.bind.address.management=0.0.0.0&quot;
# sysrc wildfly26_log_stdout=&quot;/dev/null&quot;
# sysrc wildfly26_log_stderr=&quot;/dev/null&quot;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Then start the service.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># service wildfly26 start
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Open <code>http://127.0.0.1:8080</code> to check the service status.</p><blockquote><p><strong>Optional</strong></p><p>You can also use <code>/usr/local/wildfly26/bin/add-user.sh </code> to generate an administrator account.</p></blockquote><h2 id="troubleshooting" tabindex="-1"><a class="header-anchor" href="#troubleshooting" aria-hidden="true">#</a> Troubleshooting</h2><p>If the service does not start properly, you can view the error log with <code>/usr/local/wildfly26/standalone/log/server.log</code>.</p>`,16),a=[t];function n(r,o){return l(),d("div",null,a)}var u=e(s,[["render",n],["__file","16.13-wildfly.html.vue"]]);export{u as default};
