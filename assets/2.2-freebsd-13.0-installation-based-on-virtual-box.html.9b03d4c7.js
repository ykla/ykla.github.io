import{_ as n,r as a,o as i,c as s,a as e,b as r,d as t,e as d}from"./app.3f62cd90.js";var l="/assets/QQpic20211231155133.4fd593fd.png",c="/assets/QQpic20211231155139.e2b5fda4.png";const h={},u=e("h1",{id:"_2-2-freebsd-13-0-installation-based-on-virtual-box",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#_2-2-freebsd-13-0-installation-based-on-virtual-box","aria-hidden":"true"},"#"),t(" 2.2 FreeBSD 13.0 Installation - Based on Virtual Box")],-1),p=e("p",null,[e("strong",null,"Note: This virtual machine is not recommended for novices, as there are more potholes to step through.")],-1),m=e("h2",{id:"download-virtualbox",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#download-virtualbox","aria-hidden":"true"},"#"),t(" Download VirtualBox")],-1),g=e("p",null,[t("Click "),e("code",null,"download"),t(" to download.")],-1),v={href:"https://www.virtualbox.org",target:"_blank",rel:"noopener noreferrer"},f=t("https://www.virtualbox.org"),b=d('<h2 id="installation-setup" tabindex="-1"><a class="header-anchor" href="#installation-setup" aria-hidden="true">#</a> Installation Setup</h2><p>After the installation is finished, please shut down the computer manually, uninstall or delete the installation CD, otherwise it will enter the installation screen again.</p><h2 id="network-settings" tabindex="-1"><a class="header-anchor" href="#network-settings" aria-hidden="true">#</a> Network Settings</h2><p>The network setup is complicated. In order to control the FreeBSD system in the virtual machine using the host (e.g. Windows 10), you need to set up two network cards - one in NAT mode for Internet access and the other in host-only mode for interoperability with the host. As shown in the figure.</p><p><img src="'+l+'" alt=""></p><p><img src="'+c+`" alt=""></p><p>Use the command <code># ifconfig</code> to see if the second NIC <code>em1</code> is not getting an ip address, please DHCP it manually: <code># dhclient em1</code> to get it (for long term effect add <code>ifconfig_em1=&quot;DHCP&quot;</code> to <code>/etc/rc.conf</code>).</p><p>If you don&#39;t have a network (internet) set DNS to <code>223.5.5.5</code>. If not, visit section 4 of this chapter.</p><h2 id="install-enhancement-tools" tabindex="-1"><a class="header-anchor" href="#install-enhancement-tools" aria-hidden="true">#</a> Install enhancement tools</h2><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>## pkg install virtualbox-ose-additions
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>xorg can automatically recognize the driver, no need to manually configure <code>/usr/local/etc/X11/xorg.conf</code> (after testing the manual configuration but more card, click it takes 5 seconds to move once ......).</p><p>Use <code>VBoxSVGA</code> for the graphics card controller.</p><p>Edit <code># ee etc/rc.conf</code> and add the following.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>vboxguest_enable=&quot;YES&quot;
vboxservice_enable=&quot;YES&quot;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Start the service and adjust the permissions to.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># service vboxguest restart # May indicate that the module is not found, but it does not affect the use
# service vboxservice restart
# pw groupmod wheel -m &lt;yourname&gt; # sudo permissions
# pw groupmod opt -m &lt;yourname&gt; # boot reboot permissions
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="troubleshooting" tabindex="-1"><a class="header-anchor" href="#troubleshooting" aria-hidden="true">#</a> Troubleshooting</h2><p>If you can&#39;t get the mouse in or out, press <code>ctrl</code> on the right (there is a <code>ctrl</code> on the left and right side of the keyboard, which is the default setting); if you need to restore the auto-scaling screen or can&#39;t find the menu bar, press <code>home</code> + <code>ctrl</code> on the right (hint: the <code>home</code> key is under the <code>Scroll Lock</code> on the 108 keyboard.</p>`,18);function x(_,w){const o=a("ExternalLinkIcon");return i(),s("div",null,[u,p,m,g,e("p",null,[e("a",v,[f,r(o)])]),b])}var y=n(h,[["render",x],["__file","2.2-freebsd-13.0-installation-based-on-virtual-box.html.vue"]]);export{y as default};
