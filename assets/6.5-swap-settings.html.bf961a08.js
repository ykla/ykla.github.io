import{_ as e,o as s,c as t,e as a}from"./app.cc45af49.js";const i={},n=a(`<h1 id="_6-5-swap-settings" tabindex="-1"><a class="header-anchor" href="#_6-5-swap-settings" aria-hidden="true">#</a> 6.5 SWAP settings</h1><p>If you do not set up a swap partition when you install the system, you can only do it later by dding a swap partition file. Because neither UFS nor ZFS supports shrinking file system partitions ......</p><p>dd a swap file of 1GB in size:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># dd if=/dev/zero of=/usr/swap0 bs=1M count=1024
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Set the permissions to 600, i.e. only the owner has read and write access:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># chmod 0600 /usr/swap0
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>For immediate:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># mdconfig -a -t vnode -f /usr/swap0 -u 0 &amp;&amp; swapon /dev/md0
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>In order to work after reboot, you also need to add to <code>/etc/rc.conf</code>:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>swapfile=&quot;/usr/swap0&quot;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,10),d=[n];function r(o,l){return s(),t("div",null,d)}var u=e(i,[["render",r],["__file","6.5-swap-settings.html.vue"]]);export{u as default};
