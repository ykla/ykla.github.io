import{_ as n,r as a,o as i,c as s,a as e,b as r,d as t,e as d}from"./app.4972cae2.js";var c="/assets/VBbridge.620c6a58.png",l="/assets/QQpic20211231155133.4fd593fd.png",h="/assets/QQpic20211231155139.e2b5fda4.png";const u={},p=e("h1",{id:"_2-2-freebsd-13-0-installation-based-on-virtual-box",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#_2-2-freebsd-13-0-installation-based-on-virtual-box","aria-hidden":"true"},"#"),t(" 2.2 FreeBSD 13.0 Installation - Based on Virtual Box")],-1),m=e("h2",{id:"downloading-virtualbox",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#downloading-virtualbox","aria-hidden":"true"},"#"),t(" Downloading VirtualBox")],-1),g=e("p",null,[t("Click "),e("code",null,"download"),t(" to download.")],-1),f={href:"https://www.virtualbox.org",target:"_blank",rel:"noopener noreferrer"},b=t("https://www.virtualbox.org"),v=d('<h2 id="installation-setup" tabindex="-1"><a class="header-anchor" href="#installation-setup" aria-hidden="true">#</a> Installation Setup</h2><p>After the installation is complete, please manually shut down your computer and uninstall or remove the installation CD, otherwise you will still be at the installation screen.</p><h2 id="network-settings" tabindex="-1"><a class="header-anchor" href="#network-settings" aria-hidden="true">#</a> Network settings</h2><h3 id="method-1-bridging" tabindex="-1"><a class="header-anchor" href="#method-1-bridging" aria-hidden="true">#</a> Method \u2460 Bridging</h3><p>Bridging is the simplest way to interoperate between the host and the virtual machine, and can obtain an IP address in the same IP segment as the host, such as the host is 192.168.31.123, the address of the virtual machine is 192.168.31.x. !</p><p><img src="'+c+'" alt=""></p><p>After setting <code># dhclient em0</code> you can (for long term effect add <code>ifconfig_em0=&quot;DHCP&quot;</code> in <code>/etc/rc.conf</code>).</p><p>If you don&#39;t have a network (internet) set DNS to <code>8.8.8.8</code>. If not, see other sections in this chapter.</p><h3 id="method-2-nat" tabindex="-1"><a class="header-anchor" href="#method-2-nat" aria-hidden="true">#</a> Method \u2461 NAT</h3><p>Network setup is complicated, and sometimes bridging does not always work. For the purpose of controlling the FreeBSD system in a virtual machine using a host (e.g. Windows 10), you need to set up two NICs - one in NAT network mode for Internet access and the other in host-only mode for interoperability with the host. As shown in the figure.</p><p><img src="'+l+'" alt=""></p><p><img src="'+h+`" alt=""></p><p>Use the command <code># ifconfig</code> to see if the second NIC <code>em1</code> is not getting an ip address, please DHCP it manually: <code># dhclient em1</code> to get it (for long term effect add <code>ifconfig_em1=&quot;DHCP&quot;</code> to <code>/etc/rc.conf</code>).</p><p>If you don&#39;t have a network (internet) set DNS to <code>8.8.8.8</code>. If not, see other sections in this chapter.</p><h2 id="install-enhancement-tools" tabindex="-1"><a class="header-anchor" href="#install-enhancement-tools" aria-hidden="true">#</a> Install enhancement tools</h2><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>## pkg install virtualbox-ose-additions
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>xorg can automatically recognize the driver, no need to manually configure <code>/usr/local/etc/X11/xorg.conf</code> (after testing the manual configuration but more card, click it takes 5 seconds to move once ......).</p><p>Use <code>VBoxSVGA</code> for the graphics card controller.</p><p>Edit <code># ee etc/rc.conf</code> and add the following.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>vboxguest_enable=&quot;YES&quot;
vboxservice_enable=&quot;YES&quot;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Start the service and adjust the permissions to.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># service vboxguest restart # May indicate that the module is not found, but it does not affect the use
# service vboxservice restart
# pw groupmod wheel -m &lt;yourname&gt; # sudo permissions
# pw groupmod opt -m &lt;yourname&gt; # boot reboot permissions
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="troubleshooting" tabindex="-1"><a class="header-anchor" href="#troubleshooting" aria-hidden="true">#</a> Troubleshooting</h2><p>If you can&#39;t get the mouse in or out, press <code>ctrl</code> on the right (there is a <code>ctrl</code> on the left and right side of the keyboard, which is the default setting); if you need to restore the auto-scaling screen or can&#39;t find the menu bar, press <code>home</code> + <code>ctrl</code> on the right (hint: the <code>home</code> key is under <code>Scroll Lock</code> on the 108 keyboard.</p>`,24);function x(_,w){const o=a("ExternalLinkIcon");return i(),s("div",null,[p,m,g,e("p",null,[e("a",f,[b,r(o)])]),v])}var k=n(u,[["render",x],["__file","2.2-freebsd-13.0-installation-based-on-virtual-box.html.vue"]]);export{k as default};
