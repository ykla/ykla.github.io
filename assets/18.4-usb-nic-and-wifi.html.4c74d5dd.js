import{_ as e,o as t,c as a,e as n}from"./app.6b09a359.js";const i={},o=n(`<h1 id="_18-4-usb-network-cards-and-wifi" tabindex="-1"><a class="header-anchor" href="#_18-4-usb-network-cards-and-wifi" aria-hidden="true">#</a> 18.4 USB Network Cards and WIFI</h1><p>The Raspberry Pi 4 is a relatively well configured model, so the onboard Bluetooth and WIFI are not available because the FreeBSD SDIO driver does not fully support them (you can purchase a wireless card, we recommend the COMFAST CF-WU810N).</p><p>If you have purchased the above wireless card and want to automatically connect to wifi on boot, it is very simple.</p><p>Method: <code>/boot/loader.conf</code> write:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>rtwn_usb_load=&quot;YES&quot;
legal.realtek.license_ack=1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>In <code>/etc/rc.conf</code> write:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>wlans_rtwn0=&quot;wlan0&quot;
ifconfig_wlan0=&quot;WPA DHCP&quot;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Note that in the <code>/etc/wpa_supplicant.conf</code> file (if you don&#39;t have one, create one yourself with the <code>touch</code> command) write:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>network={ ssid=&quot;wifi name&quot; psk=&quot;password&quot; }
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Just save and reboot. This will enable automatic wifi connection on boot.</p>`,10),d=[o];function s(r,c){return t(),a("div",null,d)}var u=e(i,[["render",s],["__file","18.4-usb-nic-and-wifi.html.vue"]]);export{u as default};
