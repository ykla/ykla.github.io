import{_ as e,o as t,c as i,e as n}from"./app.614c5d6a.js";const o={},s=n(`<h1 id="_5-1-fcitx-input-method-framework" tabindex="-1"><a class="header-anchor" href="#_5-1-fcitx-input-method-framework" aria-hidden="true">#</a> 5.1 Fcitx input method framework</h1><p>fcitx 5 adds support for Wayland compared to its predecessor, and is said to be smoother.</p><blockquote><p><strong>Note</strong></p><p>There are many unpredictable and strange bugs in FreeBSD-14.0-Current (fcitx5 diagnostic message English is garbled, input method shows strange Chinese characters, fcitx5-qt5 environment does not load properly\u2026\u2026), if conditions permit should be used in FreeBSD- Release refer to use this article.</p></blockquote><h2 id="fcitx-4-x" tabindex="-1"><a class="header-anchor" href="#fcitx-4-x" aria-hidden="true">#</a> Fcitx 4.X</h2><blockquote><p><strong>Note</strong></p><p>This tutorial has only been tested under KDE 5/csh.</p></blockquote><p><code># pkg install zh-fcitx zh-fcitx-configtool fcitx-qt5 fcitx-m17n zh-fcitx-libpinyin</code></p><p>Add the following configuration in <code>.cshrc</code> and <code>/etc/csh.cshrc</code>, this configuration can solve the problem that some windows fcitx is invalid.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>setenv QT4_IM_MODULE fcitx
setenv GTK_IM_MODULE fcitx
setenv QT_IM_MODULE fcitx
setenv GTK2_IM_MODULE fcitx
setenv GTK3_IM_MODULE fcitx
setenv XMODIFIERS @im=fcitx
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Add the following two lines in <code>.cshrc</code> and <code>/etc/csh.cshrc</code> to solve the problem that terminal cannot input Chinese and cannot display Chinese.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>setenv LANG zh_CN.UTF-8
setenv MM_CHARSET zh_CN.UTF-8
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Set Fcitx input method to add:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># To make the terminal uncluttered you also need to add.
setenv LANG zh_CN.UTF-8
setenv LC_CTYPE zh_CN.UTF-8
setenv LC_ALL zh_CN.UTF-8
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Autostart.</p><p><code># cp /usr/local/share/applications/fcitx.desktop ~/.config/autostart/</code></p><h2 id="fcitx-5-x" tabindex="-1"><a class="header-anchor" href="#fcitx-5-x" aria-hidden="true">#</a> Fcitx 5.X</h2><blockquote><p><strong>Note</strong></p><p>This tutorial only pass the test under KDE 5/csh.</p></blockquote><p><code># pkg install fcitx5 fcitx5-qt fcitx5-gtk fcitx5-configtool zh-fcitx5-rime zh-rime-essay zh-fcitx5-chinese-addons</code></p><p>You can also install it via ports. Environment variables depend on your window manager and desktop and shell. Tested not support slim, may be configuration problem. sddm is available.</p><p>Autostart.</p><p><code># cp /usr/local/share/applications/org.fcitx.Fcitx5.desktop ~/.config/autostart/</code></p><p>Do the following configuration in <code>.cshrc</code> and <code>/etc/csh.cshrc</code>, this configuration can solve the problem that some windows fcitx is invalid and can&#39;t input to display i18n.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>setenv QT4_IM_MODULE fcitx
setenv GTK_IM_MODULE fcitx
setenv QT_IM_MODULE fcitx
setenv GTK2_IM_MODULE fcitx
setenv GTK3_IM_MODULE fcitx
setenv XMODIFIERS @im=fcitx
setenv LANG zh_CN.UTF-8
setenv MM_CHARSET zh_CN.UTF-8
UTF-8
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Under root user, rime will not be added to input method automatically, you need to add it manually to finish initialization (find fcitx configuration tool in program, add rime input method)! For normal users, if it doesn&#39;t work, please check your shell, it should be csh, if not, please add the user to the wheel group. For other shells, please correct the environment variable for the corresponding shell.</p><p>The SLIM window will indicate that IBUS cannot find\u2026\u2026 as a suspected bug.</p><h2 id="normal-user-settings" tabindex="-1"><a class="header-anchor" href="#normal-user-settings" aria-hidden="true">#</a> Normal user settings</h2><p>The default shell for normal users is usually not <code>csh</code>, so you need to change the default shell to csh to facilitate the configuration.</p><p>First look at what the shell is: <code># echo $0</code>, if the output is not <code>csh</code>, try to change it to <code>csh</code>: <code># echo $0</code>, if the output is not <code>csh</code>, try to change it to <code>csh</code>.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># chsh -s /bin/csh
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Log out of your current account, log back in, and see if the shell changes to <code>csh</code>: <code># chsh -s /bin/csh</code></p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># echo $0
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>If <code>csh</code> is output, the configuration is successful. Then configure the rest of the environment variables in the same way as described above.</p><p><strong>Note:If you don&#39;t want to use csh, change <code>setenv</code> and other environment variables to <code>export</code>, for example <code>export GTK_IM_MODULE=fcitx</code>.</strong></p><h2 id="troubleshooting" tabindex="-1"><a class="header-anchor" href="#troubleshooting" aria-hidden="true">#</a> Troubleshooting</h2><p>If you encounter problem, please run <code>fcitx</code> troubleshooting first, but the output is only for <code>bash</code> environment variable configuration. That means the environment variables are only applicable to <code>bash</code>, <code>sh</code> and <code>zsh</code> SHELL, but not to <code>csh</code>. The environment variables for <code>csh</code> need to be configured as described above.</p><p>If you are prompted with the word <code>bash</code> and cannot output diagnostic information, you need to install <code>bash</code> first: <code># pkg install bash</code></p><h3 id="fcitx-4-x-1" tabindex="-1"><a class="header-anchor" href="#fcitx-4-x-1" aria-hidden="true">#</a> fcitx 4.x</h3><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># fcitx-diagnose
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>It is normal for fcitx 5.x to not find support for <code>GTK 4</code>.</p><h3 id="fcitx-5-x-1" tabindex="-1"><a class="header-anchor" href="#fcitx-5-x-1" aria-hidden="true">#</a> fcitx 5.x</h3><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># fcitx5-diagnose
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>For fcitx5.x, it is normal to find <code>fcitx qt 4</code> support.</p>`,41),a=[s];function d(c,r){return t(),i("div",null,a)}var h=e(o,[["render",d],["__file","5.1-fcitx-input-method-framework.html.vue"]]);export{h as default};
