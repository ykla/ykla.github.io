import{_ as n,r as s,o as a,c as i,a as e,b as d,e as r,d as o}from"./app.f03e71e5.js";const c={},l=r(`<h1 id="section-7-python-and-vscode" tabindex="-1"><a class="header-anchor" href="#section-7-python-and-vscode" aria-hidden="true">#</a> Section 7 Python and VScode</h1><p>In this section, we will assume that you have successfully set up the <code>pkg</code> tool. If you haven&#39;t, please refer to the previous section of this book to set it up.</p><h2 id="python" tabindex="-1"><a class="header-anchor" href="#python" aria-hidden="true">#</a> Python</h2><p>In FreeBSD, the different Python versions are packaged separately. Meaning, Python 3.8 and Python 3.11 are in different packages. Just like <code>llvm11</code> and <code>llvm13</code>.</p><p>However, as of FreeBSD 13, the most fully supported Python is Python 3.8. <code>pip</code> and <code>numpy</code> tools are only packaged for <code>py38</code>. That&#39;s why the dummy package <code>python3</code> points to <code>python38</code>.</p><p>So to install Python, you need to do this.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># pkg install python38 py38-pip
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Of course you can also install Python by.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># pkg install python311
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>or something like that to request a newer version of Python. Keep in mind, however, that the latest version of Python may not be as complete on FreeBSD. (Requires the use of ports)</p><p>However, it is better than some Linux distributions that never maintain a single version of Python, regardless of the consequences of doing so.</p><h2 id="vs-code" tabindex="-1"><a class="header-anchor" href="#vs-code" aria-hidden="true">#</a> VS Code</h2><p>In the old days, FreeBSD users wanted to install VS Code manually by downloading the source from GitHub, configuring their own Electron build environment, and then compiling VS Code themselves, or by using the Linux compatibility layer to run the VS Code RPM packages directly.</p><p>However, since VS Code has entered the ports tree, and thus the pkg repo, you don&#39;t have to do that anymore. Directly.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># pkg install vscode
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Then you can write code.</p>`,16),h=o("Note that the VS Code you get in this way is actually Code - OSS. The difference between Code - OSS and VS Code will not be explained here, but you can "),p={href:"https://github.com/microsoft/vscode/wiki/Differences-",target:"_blank",rel:"noopener noreferrer"},u=o("see"),y=o(" for those who are interested. between-the-repository-and-Visual-Studio-Code)."),m=e("p",null,"Just to mention that Microsoft's Python/PyLance plug-in and LLVM's clangd plug-in are known to run directly from Code - OSS and are no different from VS Code.",-1),v=e("p",null,"Microsoft's Remote Development also works with Code - OSS without any problems.",-1),f=e("p",null,"Setting up the synchronization service does not seem to work.",-1);function g(_,b){const t=s("ExternalLinkIcon");return a(),i("div",null,[l,e("p",null,[h,e("a",p,[u,d(t)]),y]),m,v,f])}var x=n(c,[["render",g],["__file","22.7-python-and-vscode.html.vue"]]);export{x as default};
