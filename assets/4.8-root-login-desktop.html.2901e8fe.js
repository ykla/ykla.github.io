import{_ as e,o as t,c as o,e as i}from"./app.eddf22e9.js";const n={},s=i(`<h1 id="_4-8-root-login-desktop" tabindex="-1"><a class="header-anchor" href="#_4-8-root-login-desktop" aria-hidden="true">#</a> 4.8 root login desktop</h1><blockquote><p><strong>Warning</strong></p><p>This section is written in the spirit of freedom, as some users may wish to log in to the desktop as root. Please be aware that the root account has the highest privileges and that misuse of the root account may <strong>damage the system</strong> and therefore use it to log in to the GUI is <strong>an extremely high security risk</strong>. Please use caution and proceed at your own risk with the following.</p></blockquote><h2 id="lightdm" tabindex="-1"><a class="header-anchor" href="#lightdm" aria-hidden="true">#</a> lightdm</h2><p>Installation and configuration.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>## pkg install lightdm-gtk-greeter lightdm
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>First set up the startup service.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># sysrc lightdm_enable=&quot;YES&quot;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Then modify the configuration file.</p><ul><li>Edit <code># ee /usr/local/etc/lightdm/lightdm.conf</code>.</li></ul><p>Scroll down and find <code>greeter-show-manual-login=true</code> remove the <code>#</code> in front of it. This line will appear several times, the first time it appears is for your introduction, do not change it, instead you should continue to scroll down.</p><ul><li>Edit <code># ee /usr/local/etc/pam.d/lightdm</code>.</li></ul><p>Comment out the line <code>account requisite pam_securetty.so</code> (add <code>#</code> to the top)</p><p>Restart the servicea:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># service lightdm restart
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>That will work.</p><h2 id="sddm" tabindex="-1"><a class="header-anchor" href="#sddm" aria-hidden="true">#</a> sddm</h2><p>Install:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># pkg install sddm
# sysrc sddm_enable=&quot;YES&quot;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Change the <code>/usr/local/etc/pam.d/sddm</code> file:</p><p>Replace <code>login</code> after <code>include</code> with <code>system</code>, 4 in total.</p><p>Restart the service:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code># service sddm restart
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>After that you can root login to sddm!</p><h4 id="note-that-the-option-in-the-lower-left-corner-of-sddm-cannot-be-wayland-it-should-be-plasma-x11-currently-kde-5-does-not-support-wayland-so-you-can-t-log-in-if-you-choose-wrong" tabindex="-1"><a class="header-anchor" href="#note-that-the-option-in-the-lower-left-corner-of-sddm-cannot-be-wayland-it-should-be-plasma-x11-currently-kde-5-does-not-support-wayland-so-you-can-t-log-in-if-you-choose-wrong" aria-hidden="true">#</a> Note that the option in the lower left corner of sddm cannot be Wayland, it should be Plasma-X11, currently KDE 5 does not support wayland, so you can&#39;t log in if you choose wrong!</h4><blockquote><p><strong>Warning</strong></p><p>The root account has the highest privileges, and using it incorrectly may <strong>damage the system</strong>, so using it to log into the GUI is <strong>an extremely high security risk</strong>.</p></blockquote>`,25),a=[s];function d(r,l){return t(),o("div",null,a)}var h=e(n,[["render",d],["__file","4.8-root-login-desktop.html.vue"]]);export{h as default};
