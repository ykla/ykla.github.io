name: 🔗 构建 pkgbase

on:
  workflow_dispatch:
  schedule:
    - cron: '0 1,9,17 * * *'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - amd64
#          - aarch64
#          - riscv64

    steps:
      - name: 检出
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: 初始化输出目录 (${{ matrix.target }})
        run: |
          mkdir -p pages-root

      - name: 在 FreeBSD VM 中构建 (${{ matrix.target }})
        uses: vmactions/freebsd-vm@v1.4.2
        with:
          release: "15.0"
          arch: x86_64
          sync-time: true
          mem: 8192
#          debug-on-error: true
          run: |
            TARGET_ARCH=${{ matrix.target }}

            rm -rf /usr/src/*
            fetch https://github.com/freebsd/freebsd-src/archive/refs/heads/main.tar.gz
            tar -xzf main.tar.gz --strip-components=1 -C /usr/src/
            cd /usr/src/

            case "$TARGET_ARCH" in
              amd64)
                sed -i '' 's/^include "std.debug"/#include "std.debug"/' sys/amd64/conf/GENERIC || true
                ;;
              aarch64)
                sed -i '' 's/^include "std.debug"/#include "std.debug"/' sys/arm64/conf/std.arm64 || true
                ;;
              riscv64)
                sed -i '' 's/^include "std.debug"/#include "std.debug"/' sys/riscv/conf/GENERIC || true
                ;;
            esac

            make -s -j$(sysctl -n hw.ncpu) TARGET_ARCH=$TARGET_ARCH buildworld
            make -s -j$(sysctl -n hw.ncpu) TARGET_ARCH=$TARGET_ARCH buildkernel
            make -s -j$(sysctl -n hw.ncpu) TARGET_ARCH=$TARGET_ARCH packages

            # 直接复制到 pages-root
            mkdir -p /home/runner/work/ykla.github.io/ykla.github.io/pages-root 
            for d in /usr/obj/usr/src/repo/FreeBSD:16:*; do mv "$d" "$(echo "$d" | sed 's/:/-/g')"; done
            cp -R /usr/obj/usr/src/repo/. /home/runner/work/ykla.github.io/ykla.github.io/pages-root/

      - name: 上传中间构建产物 (${{ matrix.target }})
        uses: actions/upload-artifact@v6
        with:
          name: freebsd-${{ matrix.target }}
          path: pages-root/
          include-hidden-files: true

  pages:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: 下载所有构建产物
        run: |
          mkdir -p pages-root
          
      - name: 下载 amd64 构建产物
        uses: actions/download-artifact@v7
        with:
          name: freebsd-amd64
          path: pages-root

#      - name: 下载 aarch64 构建产物
#        uses: actions/download-artifact@v7
#        with:
#          name: freebsd-aarch64
#          path: pages-root

#      - name: 下载 riscv64 构建产物
#        uses: actions/download-artifact@v7
#        with:
#          name: freebsd-riscv64
#          path: pages-root

      - name: 生成索引文件
        run: |
          export TZ=Asia/Shanghai
          BUILD_TIME="$(date '+%Y-%m-%d %H:%M:%S UTC+8')"
          TARGET_DIR="pages-root/FreeBSD-16-amd64"

          generate_index() {
            DIR="$1"
            OUTPUT="$DIR/index.html"

            {
              echo '<!DOCTYPE html>'
              echo '<html lang="zh-CN">'
              echo '<head>'
              echo '  <meta charset="UTF-8">'
              echo '  <meta name="viewport" content="width=device-width, initial-scale=1">'
              echo "  <title>Index of /${DIR#pages-root/}</title>"
              echo '  <style>'
              echo '    body { font-family: monospace; background:#fff; color:#000; }'
              echo '    a { color:#00e; text-decoration:none; }'
              echo '    a:hover { text-decoration:underline; }'
              echo '    table { border-collapse:collapse; }'
              echo '    th, td { padding:4px 12px; text-align:left; }'
              echo '    th { border-bottom:1px solid #000; }'
              echo '  </style>'
              echo '</head>'
              echo '<body>'
              echo "<h1>FreeBSD 中文社区 pkgbase 镜像站（已移除调试选项）</h1><h2>Index of /${DIR#pages-root/}</h2>"
              echo '<table>'
              echo '<tr><th>文件名</th><th>最后变动时间（UTC+8）</th><th>大小</th></tr>'

              if [ "$DIR" != "pages-root/FreeBSD-16-amd64" ]; then
                echo '<tr><td><a href="../">../</a></td><td></td><td></td></tr>'
              fi

              for f in "$DIR"/*; do
                NAME=$(basename "$f")

                if [ -d "$f" ]; then
                  MTIME=$(date -r "$f" '+%Y-%m-%d %H:%M:%S')
                  echo "<tr><td><a href=\"$NAME/\">$NAME/</a></td><td>$MTIME</td><td>-</td></tr>"
                else
                  SIZE=$(du -h "$f" | cut -f1)
                  MTIME=$(date -r "$f" '+%Y-%m-%d %H:%M:%S')
                  echo "<tr><td><a href=\"$NAME\">$NAME</a></td><td>$MTIME</td><td>$SIZE</td></tr>"
                fi
              done

              echo '</table>'
              echo "<hr><p>页面生成时间: $BUILD_TIME</p>"
              echo '</body></html>'
            } > "$OUTPUT"
          }

          generate_index "$TARGET_DIR"

      - name: 上传 Pages Artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: pages-root

  deploy:
    needs: pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
