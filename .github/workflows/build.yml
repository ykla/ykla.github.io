name: 🔗 构建 pkgbase

on:
  workflow_dispatch:
  schedule:
    - cron: '0 1,9,17 * * *'

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pkgbase
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - amd64
#          - aarch64
#          - riscv64

    steps:
      - name: 检出
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: 初始化输出目录 (${{ matrix.target }})
        run: |
          mkdir -p pages-root

      - name: 在 FreeBSD VM 中构建 (${{ matrix.target }})
        uses: vmactions/freebsd-vm@v1.4.2
        with:
          release: "15.0"
          arch: x86_64
          sync-time: true
          mem: 8192
#          debug-on-error: true
          run: |
            export TZ=Asia/Shanghai
            TARGET_ARCH=${{ matrix.target }}
            pkg update -f  # 注意，riscv 默认无 pkg 源
            pkg ins -y git 
            [ -d /usr/src ] || mkdir -p /usr/src && rm -rf /usr/src/*
            cd /usr/src
            git config --global advice.defaultBranchName false
            git clone --depth 1 --branch main https://git.freebsd.org/src.git /usr/src

            case "$TARGET_ARCH" in
              amd64)
                sed -i '' 's/^include "std.debug"/#include "std.debug"/' sys/amd64/conf/GENERIC || true
                ;;
              aarch64)
                sed -i '' 's/^include "std.debug"/#include "std.debug"/' sys/arm64/conf/std.arm64 || true
                ;;
              riscv64)
                sed -i '' 's/^include "std.debug"/#include "std.debug"/' sys/riscv/conf/GENERIC || true
                ;;
            esac

            #make -s -j$(sysctl -n hw.ncpu) TARGET_ARCH=$TARGET_ARCH buildworld
            #make -s -j$(sysctl -n hw.ncpu) TARGET_ARCH=$TARGET_ARCH buildkernel
            #make -s -j$(sysctl -n hw.ncpu) TARGET_ARCH=$TARGET_ARCH packages
            # 直接复制到 pages-root
            mkdir -p /home/runner/work/ykla.github.io/ykla.github.io/pages-root 
            for d in /usr/obj/usr/src/repo/FreeBSD:16:*; do mv "$d" "$(echo "$d" | sed 's/:/-/g')"; done
            cp -R /usr/obj/usr/src/repo/. /home/runner/work/ykla.github.io/ykla.github.io/pages-root/
            cd /usr/src
            TZ=Asia/Shanghai git log -1 --pretty=format:"哈希：<a href=\"https://github.com/freebsd/freebsd-src/commit/%H\" target=\"_blank\">%h</a>%n作者：%an <%ae>%n提交者：%cn <%ce>%n提交时间：%cd%n提交信息：%n%n%B" --date=format:"%Y-%m-%d %H:%M:%S %z"  > /home/runner/work/ykla.github.io/ykla.github.io/pages-root/commit.txt   
            ls -al /home/runner/work/ykla.github.io/ykla.github.io/pages-root/
            du -sh /home/runner/work/ykla.github.io/ykla.github.io/pages-root/
            
      - name: 上传中间构建产物 (${{ matrix.target }})
        uses: actions/upload-artifact@v6
        with:
          name: freebsd-${{ matrix.target }}
          path: pages-root/
          include-hidden-files: true

  pages:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: 下载所有构建产物
        run: |
          mkdir -p pages-root
          
      - name: 下载 amd64 构建产物
        uses: actions/download-artifact@v7
        with:
          name: freebsd-amd64
          path: pages-root

#      - name: 下载 aarch64 构建产物
#        uses: actions/download-artifact@v7
#        with:
#          name: freebsd-aarch64
#          path: pages-root

#      - name: 下载 riscv64 构建产物
#        uses: actions/download-artifact@v7
#        with:
#          name: freebsd-riscv64
#          path: pages-root

      - name: 生成索引文件
        run: |
          export TZ=Asia/Shanghai
          pwd
          ls -al
          ls -al */*        
          generate_index_recursive() {
            DIR="$1"
            REL_PATH="${2:-}"
            OUTPUT="$DIR/index.html"
            BUILD_TIME="$(date '+%Y-%m-%d %H:%M:%S')"

           {
                echo '<!DOCTYPE html>'
                echo '<html lang="zh-CN">'
                echo '<head>'
                echo '  <meta charset="UTF-8">'
                echo '  <meta name="viewport" content="width=device-width, initial-scale=1">'
                echo "  <title>Index of /${REL_PATH}</title>"
                echo '  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">'
                echo '  <style>'
                echo '    :root {'
                echo '      --primary-color: #000000;'
                echo '      --secondary-color: #f8f8f8;'
                echo '      --accent-color: #333333;'
                echo '      --text-color: #333333;'
                echo '      --light-text: #666666;'
                echo '      --border-color: #e0e0e0;'
                echo '      --success-color: #000000;'
                echo '      --card-bg: #ffffff;'
                echo '      --header-bg: #000000;'
                echo '      --shadow: 0 2px 8px rgba(0, 0, 0, 0.08);'
                echo '    }'
                echo '    * {'
                echo '      margin: 0;'
                echo '      padding: 0;'
                echo '      box-sizing: border-box;'
                echo '    }'
                echo '    body {'
                echo '      font-family: "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei UI", "Microsoft YaHei", "Noto Sans CJK SC", sans-serif;'
                echo '      background: none;'
                echo '      color: var(--text-color);'
                echo '      line-height: 1.6;'
                echo '    }'
                echo '    .container {'
                echo '      max-width: 1200px;'
                echo '      margin: 0 auto;'
                echo '      padding: 20px;'
                echo '    }'
                echo '    header {'
                echo '      background: none;'
                echo '      color: var(--text-color);'
                echo '      padding: 20px 0;'
                echo '      margin-bottom: 20px;'
                echo '      border-radius: 0;'
                echo '      box-shadow: none;'
                echo '    }'
                echo '    header .container {'
                echo '      padding: 0 30px;'
                echo '    }'
                echo '    h1 {'
                echo '      font-size: 1.8rem;'
                echo '      margin-bottom: 10px;'
                echo '      font-weight: 600;'
                echo '    }'
                echo '    h2 {'
                echo '      font-size: 1.8rem;'
                echo '      margin-bottom: 20px;'
                echo '      color: var(--primary-color);'
                echo '      padding-bottom: 10px;'
                echo '      border-bottom: 2px solid var(--secondary-color);'
                echo '    }'
                echo '    h3 {'
                echo '      font-size: 1.3rem;'
                echo '      margin-bottom: 15px;'
                echo '      color: var(--text-color);'
                echo '    }'
                echo '    p {'
                echo '      margin-bottom: 15px;'
                echo '      color: var(--light-text);'
                echo '    }'
                echo '    a {'
                echo '      color: var(--primary-color);'
                echo '      text-decoration: none;'
                echo '      transition: all 0.3s ease;'
                echo '    }'
                echo '    a:hover {'
                echo '      color: var(--accent-color);'
                echo '      text-decoration: underline;'
                echo '    }'
                echo '    .info-card {'
                echo '      background: var(--card-bg);'
                echo '      padding: 25px;'
                echo '      border-radius: 10px;'
                echo '      box-shadow: var(--shadow);'
                echo '      margin-bottom: 30px;'
                echo '      transition: transform 0.3s ease, box-shadow 0.3s ease;'
                echo '    }'
                echo '    .info-card:hover {'
                echo '      transform: translateY(-5px);'
                echo '      box-shadow: 0 5px 20px rgba(0, 0, 0, 0.15);'
                echo '    }'
                echo '    .commit-info {'
                echo '      background: #1f883d;'
                echo '      color: white;'
                echo '      padding: 20px;'
                echo '      border-radius: 8px;'
                echo '      border-left: 4px solid #1a7333;'
                echo '      overflow: auto;'
                echo '      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;'
                echo '      font-size: 0.9rem;'
                echo '      line-height: 1.5;'
                echo '    }'
                echo '    .table-wrapper {'
                echo '      background: var(--card-bg);'
                echo '      border-radius: 10px;'
                echo '      box-shadow: var(--shadow);'
                echo '      overflow: hidden;'
                echo '      margin-bottom: 30px;'
                echo '    }'
                echo '    table {'
                echo '      width: 100%;'
                echo '      border-collapse: collapse;'
                echo '    }'
                echo '    th {'
                echo '      background: var(--secondary-color);'
                echo '      padding: 15px;'
                echo '      text-align: left;'
                echo '      font-weight: 600;'
                echo '      color: var(--text-color);'
                echo '      border-bottom: 2px solid var(--primary-color);'
                echo '    }'
                echo '    td {'
                echo '      padding: 12px 15px;'
                echo '      border-bottom: 1px solid var(--border-color);'
                echo '      transition: background 0.2s ease;'
                echo '    }'
                echo '    tr:hover td {'
                echo '      background: var(--secondary-color);'
                echo '    }'
                echo '    tr:last-child td {'
                echo '      border-bottom: none;'
                echo '    }'
                echo '    .file-icon {'
                echo '      margin-right: 8px;'
                echo '      color: var(--primary-color);'
                echo '    }'
                echo '    .dir-icon {'
                echo '      margin-right: 8px;'
                echo '      color: var(--success-color);'
                echo '    }'
                echo '    footer {'
                echo '      background: var(--card-bg);'
                echo '      padding: 20px 0;'
                echo '      margin-top: 40px;'
                echo '      border-radius: 10px;'
                echo '      box-shadow: var(--shadow);'
                echo '      text-align: center;'
                echo '    }'
                echo '    footer p {'
                echo '      color: var(--light-text);'
                echo '      font-size: 0.9rem;'
                echo '    }'
                echo '    @media (max-width: 768px) {'
                echo '      .container {'
                echo '        padding: 10px;'
                echo '      }'
                echo '      header .container {'
                echo '        padding: 0 20px;'
                echo '      }'
                echo '      h1 {'
                echo '        font-size: 2rem;'
                echo '      }'
                echo '      h2 {'
                echo '        font-size: 1.5rem;'
                echo '      }'
                echo '      h3 {'
                echo '        font-size: 1.2rem;'
                echo '      }'
                echo '      .info-card {'
                echo '        padding: 20px;'
                echo '      }'
                echo '      th, td {'
                echo '        padding: 10px;'
                echo '        font-size: 0.9rem;'
                echo '      }'
                echo '    }'
                echo '  </style>'
                echo '</head>'
                echo '<body>'
                echo '<div class="container">'
                echo '  <header>'
                echo '    <div class="container">'
                echo '      <h1><img src="https://www.bsdcn.org/image/avatar.jpg" alt="BSDCN Logo" style="width: 40px; height: 40px; margin-right: 10px; vertical-align: middle;"> FreeBSD 中文社区 pkgbase 镜像站</h1>'
                echo '      <p>本站所有时间均为北京时间：分别在每日凌晨 1 时、上午 9 时、傍晚 5 时拉取 freebsd-src 的 main 分支（CURRENT）进行构建</p>'
                echo '      <p>本站 pkgbase 已移除调试选项 std.debug。本项目构建源代码：<a href="https://github.com/ykla/ykla.github.io" target="_blank">https://github.com/ykla/ykla.github.io</a></p>'
                echo '    </div>'
                echo '  </header>'

                echo '  <div class="info-card">'
                echo '    <h3><i class="fas fa-code-branch"></i> 当前构建提交信息</h3>'
                echo '    <div class="commit-info">'
          sed 's/&/\&amp;/g; s/</\&lt;/g; s/>/\&gt;/g' /home/runner/work/ykla.github.io/ykla.github.io/pages-root/commit.txt
                echo '    </div>'
                echo '  </div>'

                echo '  <div class="info-card">'
                echo "    <h2><i class="fas fa-folder-open"></i> Index of /${REL_PATH}</h2>"
                echo '    <div class="table-wrapper">'
                echo '      <table>'
                echo '        <tr><th><i class="fas fa-file-alt"></i> 文件名</th><th><i class="fas fa-clock"></i> 最后变动时间（UTC+8）</th><th><i class="fas fa-weight-hanging"></i> 大小</th></tr>'

          if [ "$REL_PATH" != "" ]; then
                  echo '        <tr><td><a href="../index.html"><i class="fas fa-level-up-alt"></i> ../</a></td><td></td><td></td></tr>'
          fi

          for f in "$DIR"/*; do
          NAME=$(basename "$f")
          [[ "$NAME" = "index.html" || "$NAME" = "commit.txt" ]] && continue

          MTIME=$(date -r "$f" '+%Y-%m-%d %H:%M:%S')

          if [ -d "$f" ]; then
          # 目录递归大小（排除 index.html 与 commit.txt）
          SIZE=$(find "$f" -type f ! -name "index.html" ! -name "commit.txt" -exec du -k {} + 2>/dev/null | awk '{sum+=$1} END {if(sum>=1024*1024)printf "%.2fG",sum/1024/1024; else if(sum>=1024)printf "%.2fM",sum/1024; else printf "%.2fK",sum}')
          [ -z "$SIZE" ] && SIZE="0K"
                    echo "        <tr><td><a href=\"$NAME/index.html\"><i class=\"fas fa-folder dir-icon\"></i>$NAME/</a></td><td>$MTIME</td><td>$SIZE</td></tr>"
           else
          SIZE=$(du -sh "$f" | cut -f1)
                    echo "        <tr><td><a href=\"$NAME\"><i class=\"fas fa-file file-icon\"></i>$NAME</a></td><td>$MTIME</td><td>$SIZE</td></tr>"
          fi
          done

                echo '      </table>'
                echo '    </div>'
                echo '  </div>'

                echo '  <footer>'
                echo "    <p>页面生成时间: $BUILD_TIME</p>"
                echo '    <p><i class="fas fa-copyright"></i> FreeBSD 中文社区 2026</p>'
                echo '  </footer>'
                echo '</div>'
                echo '</body>'
                echo '</html>'

          } > "$OUTPUT"

          for sub in "$DIR"/*/; do
                [ -d "$sub" ] || continue
                SUB_NAME=$(basename "$sub")
                SUB_REL_PATH="${REL_PATH}${SUB_NAME}/"
                generate_index_recursive "$sub" "$SUB_REL_PATH"
              done
          }

          generate_index_recursive "pages-root"
      - name: 上传 Pages Artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: pages-root

#      - name: 复制 artifact 到服务器
#        uses: appleboy/scp-action@v1
#        with:
#          host: ${{ secrets.HOST }}
#          username: ${{ secrets.USERNAME }}
#          password: ${{ secrets.PASSWORD }}
#          port: ${{ secrets.PORT }}
#          source: pages-root/*
#          target: ${{ secrets.HTML }}
#          rm: true

  deploy:
    needs: pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
