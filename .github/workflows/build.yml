name: 🔗 构建 pkgbase

on:
  workflow_dispatch:
  schedule:
    - cron: '0 1,9,17 * * *'

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pkgbase
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - amd64
#          - aarch64
#          - riscv64

    steps:
      - name: 检出
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: 初始化输出目录 (${{ matrix.target }})
        run: |
          mkdir -p pages-root

      - name: 在 FreeBSD VM 中构建 (${{ matrix.target }})
        uses: vmactions/freebsd-vm@v1.4.2
        with:
          release: "15.0"
          arch: x86_64
          sync-time: true
          mem: 8192
#          debug-on-error: true
          run: |
            export TZ=Asia/Shanghai
            TARGET_ARCH=${{ matrix.target }}
            pkg update -f  # 注意，riscv 默认无 pkg 源
            pkg ins -y git 
            [ -d /usr/src ] || mkdir -p /usr/src && rm -rf /usr/src/*
            cd /usr/src
            git config --global advice.defaultBranchName false
            git clone --depth 1 --branch main https://git.freebsd.org/src.git /usr/src

            case "$TARGET_ARCH" in
              amd64)
                sed -i '' 's/^include "std.debug"/#include "std.debug"/' sys/amd64/conf/GENERIC || true
                ;;
              aarch64)
                sed -i '' 's/^include "std.debug"/#include "std.debug"/' sys/arm64/conf/std.arm64 || true
                ;;
              riscv64)
                sed -i '' 's/^include "std.debug"/#include "std.debug"/' sys/riscv/conf/GENERIC || true
                ;;
            esac

            make -s -j$(sysctl -n hw.ncpu) TARGET_ARCH=$TARGET_ARCH buildworld
            make -s -j$(sysctl -n hw.ncpu) TARGET_ARCH=$TARGET_ARCH buildkernel
            make -s -j$(sysctl -n hw.ncpu) TARGET_ARCH=$TARGET_ARCH packages
            # 直接复制到 pages-root
            mkdir -p /home/runner/work/ykla.github.io/ykla.github.io/pages-root 
            for d in /usr/obj/usr/src/repo/FreeBSD:16:*; do mv "$d" "$(echo "$d" | sed 's/:/-/g')"; done
            cp -R /usr/obj/usr/src/repo/. /home/runner/work/ykla.github.io/ykla.github.io/pages-root/
            cd /usr/src
            TZ=Asia/Shanghai git log -1 --pretty=format:"哈希：%H%n作者：%an <%ae>%n提交者：%cn <%ce>%n提交时间：%cd%n提交信息：%n%n%B" --date=format:"%Y-%m-%d %H:%M:%S %z" > /home/runner/work/ykla.github.io/ykla.github.io/pages-root/commit.txt
            ls -al /home/runner/work/ykla.github.io/ykla.github.io/pages-root/
            du -sh /home/runner/work/ykla.github.io/ykla.github.io/pages-root/
            
      - name: 上传中间构建产物 (${{ matrix.target }})
        uses: actions/upload-artifact@v6
        with:
          name: freebsd-${{ matrix.target }}
          path: pages-root/
          include-hidden-files: true

  pages:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: 下载所有构建产物
        run: |
          mkdir -p pages-root
          
      - name: 下载 amd64 构建产物
        uses: actions/download-artifact@v7
        with:
          name: freebsd-amd64
          path: pages-root

#      - name: 下载 aarch64 构建产物
#        uses: actions/download-artifact@v7
#        with:
#          name: freebsd-aarch64
#          path: pages-root

#      - name: 下载 riscv64 构建产物
#        uses: actions/download-artifact@v7
#        with:
#          name: freebsd-riscv64
#          path: pages-root

      - name: 生成索引文件
        run: |
          export TZ=Asia/Shanghai
          pwd
          ls -al
          ls -al */*
        
          generate_index_recursive() {
            DIR="$1"
            REL_PATH="${2:-}"
            OUTPUT="$DIR/index.html"
            BUILD_TIME="$(date '+%Y-%m-%d %H:%M:%S')"

           {
                echo '<!DOCTYPE html>'
                echo '<html lang="zh-CN">'
                echo '<head>'
                echo '  <meta charset="UTF-8">'
                echo '  <meta name="viewport" content="width=device-width, initial-scale=1">'
                echo "  <title>Index of /${REL_PATH}</title>"
                echo '  <style>'
                echo '    body {'
                echo '      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;'
                echo '      background:#ffffff;'
                echo '      color:#000000;'
                echo '      margin:0;'
                echo '      padding:16px;'
                echo '      box-sizing:border-box;'
                echo '    }'
                echo '    h1, h2, h3 { margin-top:0; word-break:break-word; }'
                echo '    p { margin:6px 0; word-break:break-word; }'
                echo '    a { color:#0066cc; text-decoration:none; word-break:break-all; }'
                echo '    a:hover { text-decoration:underline; }'
                echo '    .table-wrapper { width:100%; overflow-x:auto; }'
                echo '    table { border-collapse:collapse; min-width:600px; width:100%; }'
                echo '    th, td { padding:6px 10px; text-align:left; white-space:nowrap; font-size:14px; }'
                echo '    th { border-bottom:1px solid #000; }'
                echo '    tr:hover td { background:#f2f2f2; }'
                echo '    hr { margin:16px 0; }'
                echo '    @media (max-width:768px) {'
                echo '      body { padding:10px; font-size:14px; }'
                echo '      th, td { padding:4px 6px; font-size:13px; }'
                echo '      table { min-width:500px; }'
                echo '    }'
                echo '  </style>'
                echo '</head>'
                echo '<body>'

                echo '<h1>FreeBSD 中文社区 pkgbase 镜像站</h1>'
                echo '<p>本站所有时间均为北京时间：分别在每日凌晨 1 时、上午 9 时、傍晚 5 时拉取 freebsd-src 的 main 分支（CURRENT）进行构建</p>'
                echo '<p>本站 pkgbase 已移除调试选项 std.debug。本项目构建源代码：https://github.com/ykla/ykla.github.io </p>'
                echo '<h3>当前构建提交信息</h3>'
                echo '<pre style="background:#f6f6f6; padding:12px; border:1px solid #ccc; overflow:auto;">'
          sed 's/&/\&amp;/g; s/</\&lt;/g; s/>/\&gt;/g' /home/runner/work/ykla.github.io/ykla.github.io/pages-root/commit.txt
                echo '</pre>'
                echo "<h2>Index of /${REL_PATH}</h2>"
                echo '<div class="table-wrapper">'
                echo '<table>'
                echo '<tr><th>文件名</th><th>最后变动时间（UTC+8）</th><th>大小</th></tr>'

          if [ "$REL_PATH" != "" ]; then
                  echo '<tr><td><a href="../index.html">../</a></td><td></td><td></td></tr>'
          fi

          for f in "$DIR"/*; do
          NAME=$(basename "$f")
          [[ "$NAME" = "index.html" || "$NAME" = "commit.txt" ]] && continue

          MTIME=$(date -r "$f" '+%Y-%m-%d %H:%M:%S')

          if [ -d "$f" ]; then
          # 目录递归大小（排除 index.html 与 commit.txt）
          SIZE=$(find "$f" -type f ! -name "index.html" ! -name "commit.txt" -exec du -k {} + 2>/dev/null | awk '{sum+=$1} END {printf "%.1fK", sum}')
          [ -z "$SIZE" ] && SIZE="0K"
                    echo "<tr><td><a href=\"$NAME/index.html\">$NAME/</a></td><td>$MTIME</td><td>$SIZE</td></tr>"
           else
          SIZE=$(du -sh "$f" | cut -f1)
                    echo "<tr><td><a href=\"$NAME\">$NAME</a></td><td>$MTIME</td><td>$SIZE</td></tr>"
          fi
          done

                echo '</table>'
                echo '</div>'

                echo "<hr><p>页面生成时间: $BUILD_TIME</p>"
                echo '<p>FreeBSD 中文社区 2026</p>'
                echo '</body>'
                echo '</html>'

          } > "$OUTPUT"

          for sub in "$DIR"/*/; do
                [ -d "$sub" ] || continue
                SUB_NAME=$(basename "$sub")
                SUB_REL_PATH="${REL_PATH}${SUB_NAME}/"
                generate_index_recursive "$sub" "$SUB_REL_PATH"
              done
          }

          generate_index_recursive "pages-root"
      - name: 上传 Pages Artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: pages-root

#      - name: 复制 artifact 到服务器
#        uses: appleboy/scp-action@v1
#        with:
#          host: ${{ secrets.HOST }}
#          username: ${{ secrets.USERNAME }}
#          password: ${{ secrets.PASSWORD }}
#          port: ${{ secrets.PORT }}
#          source: pages-root/*
#          target: ${{ secrets.HTML }}
#          rm: true

  deploy:
    needs: pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
