name: 🔗 构建 pkgbase

on:
  workflow_dispatch:
  schedule:
    - cron: '0 1,9,17 * * *'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch:
          - x86_64
          - aarch64
          - riscv64

    steps:
      - name: 检查配置
        run: |
          date
          uname -r
          free -hg
          pwd

      - name: 检出
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: 在 FreeBSD VM 中构建 (${{ matrix.arch }})
        uses: vmactions/freebsd-vm@v1.3.8
        with:
          release: "15.0"
          arch: ${{ matrix.arch }}
          sync-time: true
          mem: 12288
          # 条件传参：riscv64 使用 sync: scp，否则默认
          sync: ${{ matrix.arch == 'riscv64' && 'scp' || 'true' }}
          debug-on-error: true
          run: |
            rm -rf /usr/src/*
            fetch https://github.com/freebsd/freebsd-src/archive/refs/heads/main.tar.gz
            tar -xzf main.tar.gz --strip-components=1 -C /usr/src/
            cd /usr/src/
            echo '注释 amd64 中的开发功能'
            grep -n 'include "std.debug"' /usr/src/sys/amd64/conf/GENERIC
            sed -i '' 's/^include "std.debug"/#include "std.debug"/' /usr/src/sys/amd64/conf/GENERIC
            echo '检查 amd64 中的开发功能是否被成功注释'
            grep -n '^#include "std.debug"' /usr/src/sys/amd64/conf/GENERIC
            echo '注释 arm64 中的开发功能'
            grep -n 'include "std.debug"' /usr/src/sys/arm64/conf/std.arm64
            sed -i '' 's/^include "std.debug"/#include "std.debug"/' /usr/src/sys/arm64/conf/std.arm64
            echo '检查 arm64 中的开发功能是否被成功注释'
            grep -n '^#include "std.debug"' /usr/src/sys/arm64/conf/std.arm64
            echo '注释 riscv 中的开发功能'
            grep -n 'include "std.debug"' /usr/src/sys/riscv/conf/GENERIC
            sed -i '' 's/^include "std.debug"/#include "std.debug"/' /usr/src/sys/riscv/conf/GENERIC
            echo '检查 armriscv64 中的开发功能是否被成功注释'
            grep -n '^#include "std.debug"' /usr/src/sys/riscv/conf/GENERIC
            echo '开始构建世界'
            make -s -j5 buildworld
            echo '开始构建内核'
            make -s -j5 buildkernel
            echo '开始构建软件包'
            make -s -j5 packages
            DEST=/home/runner/work/ykla.github.io/ykla.github.io/pages-root/freebsd-base
            mkdir -p "$DEST"
            cp -R /usr/obj/usr/src/repo/. "$DEST/"

      - name: 检查构建产物
        run: |
          ls -al pages-root/
          du -hs pages-root/
          echo "# 当前软件包构建完成时间（北京时间）"$'\n\n'"$(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M:%S')" > pages-root/README.md

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          path: pages-root
