name: 🔗 构建 pkgbase

on:
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch:
          - x86_64
          - aarch64
          - riscv64

    steps:
      - name: 查看配置
        run: |
          date
          uname -r
          free -hg
          pwd

      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Build inside FreeBSD VM (${{ matrix.arch }})
        uses: vmactions/freebsd-vm@v1.3.8
        with:
          release: "15.0"
          arch: ${{ matrix.arch }}
          sync-time: true
          mem: 12288
          # 条件传参：riscv64 使用 sync: scp，否则默认
          sync: ${{ matrix.arch == 'riscv64' && 'scp' || 'true' }}
          run: |
            rm -rf /usr/src/*
            fetch https://github.com/freebsd/freebsd-src/archive/refs/heads/main.tar.gz
            tar -xzf main.tar.gz --strip-components=1 -C /usr/src/
            cd /usr/src/
            grep -n 'include "std.debug"' /usr/src/sys/amd64/conf/GENERIC
            sed -i '' 's/^include "std.debug"/#include "std.debug"/' /usr/src/sys/amd64/conf/GENERIC
            grep -n '^#include "std.debug"' /usr/src/sys/amd64/conf/GENERIC
            grep -n 'include "std.debug"' /usr/src/sys/arm64/conf/GENERIC
            sed -i '' 's/^include "std.debug"/#include "std.debug"/' /usr/src/sys/arm64/conf/GENERIC
            grep -n '^#include "std.debug"' /usr/src/sys/arm64/conf/GENERIC
            grep -n 'include "std.debug"' /usr/src/sys/riscv/conf/GENERIC
            sed -i '' 's/^include "std.debug"/#include "std.debug"/' /usr/src/sys/riscv/conf/GENERIC
            grep -n '^#include "std.debug"' /usr/src/sys/riscv/conf/GENERIC
            make -j8 buildworld
            make -j8 buildkernel
            make -j8 packages
            DEST=/home/runner/work/ykla.github.io/ykla.github.io/pages-root/freebsd-base/
            mkdir -p "$DEST"
            cp -R -p /usr/obj/usr/src/repo/. "$DEST/"

      - name: 查看构建产物
        run: |
          ls -al pages-root/
          du -hs pages-root/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          path: pages-root
